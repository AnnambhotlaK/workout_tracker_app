<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.onfinity.onfinity">

    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- App icon in build.gradle (Comment moved here) -->
    <application
        android:icon = "@mipmap/ic_launcher"
        android:label="Onfinity"
        android:name="${applicationName}"
        tools:replace = "android:label, android:icon, android:name">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
                <intent-filter>
                    <action android:name="android.intent.action.MAIN"/>
                    <category android:name="android.intent.category.LAUNCHER"/>
                </intent-filter>
        </activity> <!-- Activity tag correctly closes before other application-level meta-data -->
    <!-- This meta-data is at the application level, which is correct -->
    <meta-data
        android:name="flutterEmbedding"
        android:value="2"
    />

    </application> <!-- Correct closing tag for application -->
</manifest>
